// ═══════════════════════════════════════════════════════════════════
// ═══ STEP 4 — Alive!: buttonMash mini-game ═══════════════════════
// ═══════════════════════════════════════════════════════════════════

#include <Arduino.h>
#include "game.h"

int32_t buttonMash(int32_t pin, uint32_t durationMs) {
    // TODO: count button presses on `pin` over `durationMs` milliseconds.
    // Hint: use millis() for the timer and edge detection for each press.
    return 0;
}
