// ═══════════════════════════════════════════════════════════════════
// ═══ STEP 1 — Buttons: pin setup, edge detection, actions ════════
// ═══════════════════════════════════════════════════════════════════

#include <Arduino.h>
#include "buttons.h"

void setupButtons() {
    // TODO: set pinMode for each button pin (INPUT_PULLDOWN)
}

void readButtons(Pet& pet) {
    // TODO: read each button, detect rising edge, call pet actions
}
